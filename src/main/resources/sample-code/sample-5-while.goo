procedure foo(a,b,c)
{		
	SYS = _world.core.system;
	i = 0;
	while(i.lt(100)){
		
		//bar.foo();
		i.inc(1);	
		
		if(i.equals(10))
			break;
		
		if(i.equals(20))	
			continue;	
	}	
	
	SYS.console.print(i);
}
