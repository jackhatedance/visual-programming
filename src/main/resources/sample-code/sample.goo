procedure foo(a,b,c)
{

	//this is single line comment

	//declare links(actors)	
	var a,b,c;
	
	//init, evalueation of any expr is object
	x = 2;
	y = "a string";
	z = true;
	
	//a -> b; via ID; hard link
	a.linkTo(b);
	//soft link
	a.linkTo("parent.b.c");

	a = a.bar(x.y.z(),a.b().x);

	if(a.equals(b)){
		//do something
		while(false){
			a=b.bar();
		}
	}
	
	result = "我的名字是".concat(self.name);
	result = self.name;


}


